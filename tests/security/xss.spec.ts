import { test, expect } from '@playwright/test';\n\nconst maliciousInput = '<script>alert(1)</script>';\n\ntest('TC-07 XSS Prevention', async ({ page }) => {\n  await page.goto('/');\n  await page.fill('input[placeholder="Search"]', maliciousInput);\n  await page.press('input[placeholder="Search"]', 'Enter');\n  // Ensure the page does not contain an executed script tag\n  const scriptTags = await page.$$('script');\n  expect(scriptTags.length).toBe(0);\n});\n